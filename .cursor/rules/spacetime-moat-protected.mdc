---
description: Fully proprietary — no OSS pushes; product IP stays local
alwaysApply: true
---

# Space-Time protected — proprietary mode

**Locked (2026-08-26):** Morph-head / Body+Cloth product path is **not** open-repo work. Stay local. No public OSS replacement story. User-facing proprietary app comes later.

## Do not push

1. **Do not** `git push` OpenNexus3DStudio or 3DAIGC-API to GitHub (or any public remote) unless the user **explicitly** orders a specific push in that turn.
2. **Never** `git add` / commit / push: `src/moat/`, `3DAIGC-API/assets/moat/`, `MONETIZATION_ROADMAP.md`, `FUTURE_RD.md`, `MESH_WRAP_ROADMAP.md` (API root), `Pitch Deck/`, `memory-bank/`, `.brain/`, `.sessionmem-team/`, `backups/`, companion proxy scripts, `.env`, `.cursor/mcp.json`, secrets, `scripts/staging-*/`, `scripts/tmp-*`.
3. **Never** invent a public stand-in for the wrap template. **Never** name vendors, kit repos, or internal template filenames in tracked public docs or chat replies.

See also: `.cursor/rules/secrets-moat-memory-protection.mdc` (full memory/secrets table).

## Agent rules

1. Default new product IP to gitignored local paths. Sync DGX ↔ Surface via `sync-changes-to-pc.sh` / scp — **not** git remotes.
2. Public product copy for photo→avatar: **TRELLIS to VRM** (task: Avatar from Image). Do not describe internal wrap/vendor stack.
3. License gate still applies (`MODEL_LICENSES.md` / R&D gate) before shipping commercial features.
4. Forks / old OSS remotes are frozen for this track; do not expand them with unreleased work.

## Revenue / strategy

Internal detail only: `memory-bank/`, `MONETIZATION_ROADMAP.md` — never paste into tracked public docs.
